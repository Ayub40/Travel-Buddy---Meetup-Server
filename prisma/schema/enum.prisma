enum UserRole {
    SUPER_ADMIN
    USER
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum Gender {
    MALE
    FEMALE
    OTHER
}

enum TravelType {
    SOLO
    FAMILY
    FRIENDS
}

enum PaymentStatus {
    PENDING
    SUCCESS
    FAILED
}

enum JoinStatus {
    PENDING
    ACCEPTED
    REJECTED
}
